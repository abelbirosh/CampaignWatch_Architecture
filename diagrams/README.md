# Diagrams

Keep the source next to the export:

- `*.mmd` — Mermaid source (GitHub renders these inline in Markdown too)
- `*.dsl` / `*.json` — Structurizr / C4 workspace
- `*.png` / `*.svg` — exported images referenced from writeups
